[pytest]
# --doctest-modules makes pytest import every .py file it walks into, so collection
# must be restricted. Without testpaths a bare `pytest` from the repo root also
# imports benchmarks/, whose scripts import triqs/pydlr at module level and fail
# to collect in environments where those are not installed.
testpaths = src test
addopts = --doctest-modules
